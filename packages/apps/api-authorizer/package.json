{
    "name": "api-authorizer",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "build": "rm identity-authorizer.zip; tsc && cd lib && zip -r ../identity-authorizer.zip ./ && cd ../ && zip -ur identity-authorizer.zip node_modules",
        "publish-artifact": "yarn build && aws s3 cp ./identity-authorizer.zip s3://identity-dist/lambdas/identity-authorizer-${PUBLISH_VERSION}.zip"
    },
    "dependencies": {
        "aws-serverless-express": "^3.3.8",
        "express": "^4.17.1"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.64",
        "@types/aws-serverless-express": "^3.3.3",
        "@types/express": "^4.17.9",
        "@weco/auth0-client": "*",
        "ts-node": "^9.0.0",
        "typescript": "^4.0.5"
    }
}
