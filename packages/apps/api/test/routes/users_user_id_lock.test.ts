describe('/users/{userId}/lock', () => {
  describe('PUT /users/{userId}/lock', () => {
    it('locks a user in Auth0', () => {});
    it('404s for users that do not exist', () => {});
  });

  describe('DELETE /users/{userId}/lock', () => {
    it('unlocks a user in Auth0', () => {});
    it('404s for users that do not exist', () => {});
  });
});
