describe('/users/{userId}/password', () => {
  describe('PUT /users/{userId}/password', () => {
    it('changes a user password', () => {});
    it('404s for users that do not exist', () => {});
  });
});
