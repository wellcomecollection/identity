describe('/users', () => {
  describe('GET /users', () => {
    it('fetches a list of users', () => {});
    it('can filter users by status', () => {});
  });

  describe('POST /users', () => {
    it('creates a user', () => {});
    it('does not create a user that already exists in Sierra', () => {});
    it('does not create a user that already exists in Auth0', () => {});
  });
});
