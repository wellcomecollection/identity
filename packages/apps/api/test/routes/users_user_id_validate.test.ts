describe('/users/{userId}/validate', () => {
  describe('POST /users/{userId}/validate', () => {
    it('validates a correct user password', () => {});
    it('401s for an incorrect user password', () => {});
    it('404s for users that do not exist', () => {});
  });
});
