{
  "summary": "Searches for users",
  "description": "Executes a search for users using the `query` parameter, limited to `size` results per page. Multiple pages of results can be paged through using the `page` parameter.",
  "tags": [
    "User"
  ]
}
