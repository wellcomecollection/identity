{
  "summary": "Searches for users",
  "description": "Executes a search for users using the `name`, `email` and `status` (`active`, `locked` and `deletePending`) parameters, limited to `pageSize` results per page. Multiple pages of results can be paged through using the `page` parameter, and sorted using the `sort` parameter ordered by `sortDir`.",
  "tags": [
    "Administrator"
  ]
}
